export const MIN_SCORE = 0;
export const MAX_SCORE = 10;

/** Guard against a model returning an unbounded suggestion list. */
export const MAX_SUGGESTIONS = 5;

export const DEFAULT_RATING_PAGE_SIZE = 20;
export const MAX_RATING_PAGE_SIZE = 50;
